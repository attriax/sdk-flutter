# Vendored KMP core (reproducible build artifact of sdk-kmp), copied in for local dev.
attriax_flutter_ios/Frameworks/
# SwiftPM: the FlutterFramework package is generated by Flutter tooling at build time and
# injected into the app-level aggregate (plugin Package.swift does NOT depend on it), plus
# SPM build/checkout dirs.
FlutterFramework/
.build/
.swiftpm/
