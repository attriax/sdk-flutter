# Vendored KMP core (reproducible build artifact of sdk-kmp), copied in for local dev.
attriax_flutter_ios/Frameworks/
# SwiftPM: the Flutter framework package is generated by Flutter tooling at build time
# (Package.swift depends on ../FlutterFramework), plus SPM build/checkout dirs.
FlutterFramework/
.build/
.swiftpm/
