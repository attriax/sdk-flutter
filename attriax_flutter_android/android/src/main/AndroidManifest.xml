<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Runtime permissions (INTERNET / ACCESS_NETWORK_STATE / INSTALL_REFERRER /
        AD_ID) are declared by the shared KMP core AAR (com.attriax:core) and
        inherited here via Gradle manifest merging. Only genuinely Flutter-plugin
        concerns remain below: the deep-linking opt-out and the in-app WebView
        browser Activity backing the plugin's openBrowserUrl API (the engine only
        fires an external ACTION_VIEW; the in-app WebView is wrapper-owned UX).
    -->
    <application>
        <meta-data android:name="flutter_deeplinking_enabled" android:value="false" />
        <activity
            android:name=".AttriaxInAppBrowserActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar" />
    </application>
</manifest>
