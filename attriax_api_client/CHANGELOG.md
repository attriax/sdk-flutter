# Changelog

## 0.6.0

- Deprecated. The Attriax Flutter SDK now runs on the shared native engine and no longer depends on this generated client. The package is retained and version-aligned for pub.dev/federation compatibility only; do not use it in new code.

## 0.5.0

- Initial generated Dart API client package for the Attriax Flutter SDK.
