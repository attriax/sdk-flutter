name: example_gdpr
description: Minimal manual GDPR example for the Attriax Flutter SDK.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  attriax_flutter:
    path: ../attriax

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
