# attriax_flutter_windows_example

Minimal Windows example application for the `attriax_flutter_windows` plugin.

Use this example to verify Windows plugin registration and the basic native
method-channel round-trip during local development and release validation.
